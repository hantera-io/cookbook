from {
  sendCompleteReservationRequest = reservationRequestId =>
    message(
      'reservationRequest',
      $'{reservationRequestId}',
      'complete'
      {

      }
    ) then result => result match
      text |> return $'Result was {result}'
      |> return $'Unexpected result: {typeof result}'
}